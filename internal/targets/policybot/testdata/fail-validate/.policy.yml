# Policy bot configuration
# https://github.com/palantir/policy-bot#configuration
policy:
  approval:
    - or:
        - Dependabot update
        # this is not defined below, so this fails validation
        - Code change

approval_rules:
  # This rule matches when a pr is created by dependabot
  - name: Dependabot update
    if:
      only_has_contributors_in:
        users:
          - "dependabot[bot]"
      only_changed_files:
        paths:
          - "^.github/workflows/.*$"
          - "^devtools/Dockerfile$"
      has_valid_signatures_by_keys:
        key_ids: ["B5690EEEBB952194"]
    requires:
      count: 1 # A Dependabot pr requires 1 approval of review bot or a user
      teams:
        - "coopnorge/cloud-platform"
      users:
        - "coop-norge-review-bot"
    options:
      invalidate_on_push: true
      request_review:
        enabled: true
        mode: random-users
        count: 2 # The number of random-users to request reviews from
      methods:
        github_review: true
        comments: []
